{
  "name": "cadiilac-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Cadiilac AI — notes, drive and a study assistant in one workspace.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js",
    "start": "node scripts/serve.js",
    "lint": "node --experimental-vm-modules --no-warnings scripts/check-syntax.js"
  },
  "engines": {
    "node": ">=18"
  }
}
